# Machine Status

Machine Status owns the bottom dashboard status strip. It evaluates the nine
machine-readiness indicators from read-only subsystem snapshots and applies the
ordered gray/green/red display rule.
